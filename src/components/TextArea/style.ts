import styled from 'styled-components';

export const StyledTextArea = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  position: absolute;
  padding-right: 500px;
  left: 0;
  top: 0;
  width: 60%;
  height: 100%;
  background: linear-gradient(to right, #000 3%, rgba(0, 0, 0, 0) 100%);
`;
